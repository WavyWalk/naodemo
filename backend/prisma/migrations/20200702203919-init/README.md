# Migration `20200702203919-init`

This migration has been generated by hans at 7/2/2020, 8:39:19 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
"id" SERIAL,"identifier" text   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Account" (
"email" text   ,"id" SERIAL,"password" text   ,"userId" integer   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Role" (
"id" SERIAL,"name" text   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Event" (
"additionalInformation" text   ,"description" text   ,"eventId" text   ,"eventOrganiserId" integer   ,"id" SERIAL,"maxAtendees" integer   ,"price" Decimal(65,30)   ,"title" text   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."EventField" (
"id" SERIAL,"name" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."EventTargetGroup" (
"id" SERIAL,"name" text   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Category" (
"id" SERIAL,"name" text   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."EventDate" (
"date" timestamp(3)   ,"eventId" integer   ,"id" SERIAL,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."EventAddress" (
"city" text   ,"country" text   ,"eventId" integer   ,"geo" jsonb   ,"id" SERIAL,"street" text   ,"streetNumber" text   ,"zipCode" text   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."EventOrganiser" (
"id" SERIAL,"userId" integer   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."EventContact" (
"email" text   ,"eventId" integer   ,"id" SERIAL,"name" text   ,"phone" text   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."_RoleToUser" (
"A" integer  NOT NULL ,"B" integer  NOT NULL )

CREATE TABLE "public"."_EventToEventField" (
"A" integer  NOT NULL ,"B" integer  NOT NULL )

CREATE TABLE "public"."_EventToEventTargetGroup" (
"A" integer  NOT NULL ,"B" integer  NOT NULL )

CREATE TABLE "public"."_CategoryToEvent" (
"A" integer  NOT NULL ,"B" integer  NOT NULL )

CREATE UNIQUE INDEX "Account_userId" ON "public"."Account"("userId")

CREATE UNIQUE INDEX "EventAddress_eventId" ON "public"."EventAddress"("eventId")

CREATE UNIQUE INDEX "EventContact_eventId" ON "public"."EventContact"("eventId")

CREATE UNIQUE INDEX "_RoleToUser_AB_unique" ON "public"."_RoleToUser"("A","B")

CREATE  INDEX "_RoleToUser_B_index" ON "public"."_RoleToUser"("B")

CREATE UNIQUE INDEX "_EventToEventField_AB_unique" ON "public"."_EventToEventField"("A","B")

CREATE  INDEX "_EventToEventField_B_index" ON "public"."_EventToEventField"("B")

CREATE UNIQUE INDEX "_EventToEventTargetGroup_AB_unique" ON "public"."_EventToEventTargetGroup"("A","B")

CREATE  INDEX "_EventToEventTargetGroup_B_index" ON "public"."_EventToEventTargetGroup"("B")

CREATE UNIQUE INDEX "_CategoryToEvent_AB_unique" ON "public"."_CategoryToEvent"("A","B")

CREATE  INDEX "_CategoryToEvent_B_index" ON "public"."_CategoryToEvent"("B")

ALTER TABLE "public"."Account" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Event" ADD FOREIGN KEY ("eventOrganiserId")REFERENCES "public"."EventOrganiser"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."EventDate" ADD FOREIGN KEY ("eventId")REFERENCES "public"."Event"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."EventAddress" ADD FOREIGN KEY ("eventId")REFERENCES "public"."Event"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."EventOrganiser" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."EventContact" ADD FOREIGN KEY ("eventId")REFERENCES "public"."Event"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."_RoleToUser" ADD FOREIGN KEY ("A")REFERENCES "public"."Role"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_RoleToUser" ADD FOREIGN KEY ("B")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_EventToEventField" ADD FOREIGN KEY ("A")REFERENCES "public"."Event"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_EventToEventField" ADD FOREIGN KEY ("B")REFERENCES "public"."EventField"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_EventToEventTargetGroup" ADD FOREIGN KEY ("A")REFERENCES "public"."Event"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_EventToEventTargetGroup" ADD FOREIGN KEY ("B")REFERENCES "public"."EventTargetGroup"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_CategoryToEvent" ADD FOREIGN KEY ("A")REFERENCES "public"."Category"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_CategoryToEvent" ADD FOREIGN KEY ("B")REFERENCES "public"."Event"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200702203919-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,105 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+    id Int @default(autoincrement()) @id
+    account Account?
+    identifier String?
+    roles Role[] @relation(references: [id])
+    eventOrganisers EventOrganiser[]
+}
+
+model Account {
+    id Int @default(autoincrement()) @id
+    userId Int?
+    user User? @relation(fields: [userId], references: [id])
+    email String?
+    password String?
+}
+
+
+model Role {
+    id Int @default(autoincrement()) @id
+    name String?
+    users User[] @relation(references: [id])
+}
+
+model Event {
+    id Int @default(autoincrement()) @id
+    price Float?
+    title String?
+    eventId String?
+    maxAtendees Int?
+    eventFields EventField[] @relation(references: [id])
+    eventTargetGroups EventTargetGroup[] @relation(references: [id])
+    categories Category[] @relation(references: [id])
+    eventDates EventDate[]
+    eventAddresses EventAddress?
+    eventOrganiserId Int?
+    eventOrganiser EventOrganiser? @relation(fields: [eventOrganiserId], references: [id])
+    eventContact EventContact?
+    additionalInformation String?
+    description String?
+}
+
+model EventField {
+    id Int @default(autoincrement()) @id
+    name String
+    events Event[] @relation(references: [id])
+}
+
+model EventTargetGroup {
+    id Int @default(autoincrement()) @id
+    events Event[] @relation(references: [id])
+    name String?
+}
+
+model Category {
+    id Int @default(autoincrement()) @id
+    events Event[] @relation(references: [id])
+    name String?
+}
+
+model EventDate {
+    id Int @default(autoincrement()) @id
+    eventId Int?
+    event Event? @relation(fields: [eventId], references: [id])
+    date DateTime?
+}
+
+model EventAddress {
+    id Int @default(autoincrement()) @id
+    eventId Int?
+    event Event? @relation(fields: [eventId], references: [id])
+    country String?
+    city String?
+    street String?
+    streetNumber String?
+    zipCode String?
+    geo Json?
+}
+
+model EventOrganiser {
+    id Int @default(autoincrement()) @id
+    events Event[]
+    userId Int?
+    user User? @relation(fields: [userId], references: [id])
+}
+
+model EventContact {
+    id Int @default(autoincrement()) @id
+    eventId Int?
+    event Event? @relation(fields: [eventId], references: [id])
+    name String?
+    phone String?
+    email String?
+}
```


